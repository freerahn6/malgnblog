[build]
  command = "pip install -r requirements.txt && python build.py"
  publish = "_deploy/public"

[build.environment]
  PYTHON_VERSION = "3.11"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
