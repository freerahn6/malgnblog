# 맑은소프트 블로그 (교육운영 노트)

## 콘솔에서 글쓰기 — 새 글·수정 구현 (2026-08-12)
- **무엇**: `/gamma2` 콘솔에서 **새 글 작성**과 **기존 글 수정**이 된다. 작성자를 고르면 네임택이
  자동으로 따라오고, 본문은 위지윅(TOAST UI, 마크다운 탭 겸용)으로 쓴다. 기획서 Phase 2 해당분.
- **저장 경로**: 콘솔 → `POST /api/stats?write=1` → `write.jsp` → **GitHub `main` 커밋** →
  Actions → `deploy` → **[프로그램 갱신]**. 서버 로컬에 쓰지 않는 이유는 `update.jsp` 의
  `git reset --hard` 가 지우기 때문(기획서 F2). **저장 ≠ 발행** — 갱신 버튼까지 눌러야 뜬다.
- **경로 우회**: Apache 가 `/api/` 를 통째로 프록시하지 않아 `/api/posts/write` 는 404 다 →
  이미 프록시되는 `/api/stats` 에 `?write=1` 로 얹고 stats.jsp 가 forward 한다(`?posts=1` 과 같은 수법).
  web.xml 에 정식 매핑도 걸어 뒀다 — blanket `/api/` 가 적용되면 그대로 쓸 수 있다.
- **비번을 POST 본문으로**: 쓰기 권한이 붙은 요청이라 쿼리스트링에 실으면 액세스로그에 평문으로 남는다(F4).
- **front matter 원문 보존**: 통째 재직렬화가 아니라 **아는 키의 그 줄만 교체**한다.
  실측: 원고 41편 전부를 콘솔 로직으로 왕복시켜 build.py 의 `parse()` 로 대조 → **불일치 0건**
  (faq·tags·cluster 소실 없음, 본문 무변형).
- **표·SVG 방어**: 본문에 원시 HTML 이 있으면 위지윅을 막고 마크다운 탭으로 고정한다(`hasRawHtml`).
  위지윅이 그 블록을 재작성하면 글이 조용히 깨진다.
- **손으로 짠 base64·JSON 파서 검증**: JDK 가 없어 JSP 실행은 못 하므로 알고리즘을 동일 이식해
  표준 구현과 대조(입력 209종·최대 28KB, 개행 섞인 GitHub 응답 형식 왕복) → 일치.
- **⚠ 남은 설정 1건**: 서버 `/home/blog/data/github.token`(fine-grained PAT, Contents:write, 0600).
  없으면 저장이 `503 no_token`. 절차는 `deploy/서버담당자-설치가이드.md` **3-7**.
  **관리자 비번이 아직 `gamma` 면 이번에 반드시 교체** — 이제 그 비번으로 글을 쓰고 고칠 수 있다.
- **범위 밖(그대로 둠)**: 목록의 온오프 토글·삭제·복원(기획서 Phase 1 잔여), 이미지 업로드(Phase 3).
  편집 화면 안에서는 게시/비게시 토글이 된다(front matter `draft`).
> 갱신: 2026-08-04 · 33편 발행 · 배포: **자체 웹서버(Rocky 9.5 / Apache 2.4.62 / Resin 4.0.67)** — 코드·문서 준비 완료, 담당자 설치 대기

## 예약 발행 기능 (2026-08-04)
- **무엇**: 원고를 미리 올려두고 지정한 일시에 자동 노출. front matter `publish_at: 2026-08-10 09:00`(KST).
  그 시각 전까지는 draft 와 똑같이 **사이트 출력에서 전량 제외**(페이지·홈·목록·sitemap·RSS). 사용법은 `CLAUDE.md`.
- **왜 세 군데를 고쳤나**: 정적 사이트라 "시각이 되면 뜬다"가 저절로 되지 않는다. ①빌드가 시각으로 숨기고
  ②예약 시각 이후 빌드가 다시 돌고 ③서버가 새 결과물을 당겨가야 비로소 노출된다. 셋 다 이어야 동작한다.
  1. `build.py` — `publish_at` 파싱·숨김. **형식 오류는 빌드 중단**(오타를 무시하면 예약 글이 즉시 공개된다).
     `date` 는 publish_at 기준으로 자동 정렬(어긋나게 적으면 덮어쓰고 경고). posts.json 에 `status`(published/scheduled/draft)·`publish_at` 추가
  2. `deploy.yml` — `schedule: */10 * * * *` 정기 빌드 추가. 내용이 그대로면 커밋 없이 끝나 서버는 조용하다
  3. `deploy.yml` — **서버 갱신 요청 단계 신설**. 배포에 변화가 있을 때만 `/api/update` 를 호출한다
- **⚠ 남은 설정 1건**: 저장소 Secret **`BLOG_ADMIN_PW`** 등록(= 서버 `/home/blog/data/admin.pw` 값).
  **없으면 예약 시각이 와도 서버에 반영되지 않는다** — 사람이 `/gamma` [프로그램 갱신] 버튼을 눌러야 뜬다.
  (secret 없으면 그 단계는 조용히 건너뛰므로 기존 동작은 그대로. 배포가 깨지지는 않는다)
- **노출 시각 오차**: GitHub schedule 은 정시를 보장하지 않는다(혼잡 시 지연·건너뜀) → **예약 시각 + 최대 15분**으로 안내할 것.
  또한 **저장소에 60일간 커밋이 없으면 GitHub이 schedule 을 자동 정지**시킨다(Actions 탭에서 재활성 필요)
- **곁다리로 고친 것 2건**: ① 건전성 검사의 원고 수 기준이 `articles/*.md` 개수여서 draft·예약 글까지 세어
  멀쩡한 빌드를 실패시킬 수 있었다 → build.py 가 남기는 실제 발행 편수(`_deploy/published-count.txt`)로 교체
  ② `pages` 출력이 엉뚱한 스텝에 있어 빌드 요약의 "페이지: 개"가 늘 비어 있었다 → 검사 스텝으로 이동
- **posts.json `generated` 를 벽시계 → 콘텐츠 기준으로 변경**: 10분마다 빌드가 도는데 매번 값이 바뀌면
  내용이 같아도 deploy 브랜치에 빈 커밋이 쌓이고 서버가 헛되이 계속 당겨간다
- **관리자 콘솔 `/gamma2`**: `🕘 예약` 배지·상태 필터·대시보드 "예약 N편 대기중"·목록에 공개 시각 표기
- **예약글 전용 화면 `#/scheduled` 추가(08-04)**: 상단 메뉴 `예약글`(대기 편수 배지, 0이면 숨김).
  공개 임박 순 정렬 + 남은 시간("2일 3시간 뒤"). 예약 시각이 지났는데 아직 빌드가 안 돈 글은
  **"공개 시각 지남 · 반영 대기"** 로 구분해 보여준다 — 이 구분이 없으면 "시각이 지났는데 왜 안 뜨냐"는
  오해가 난다(매니페스트는 빌드 시점의 사진이라 다음 빌드 전까지 예약 상태로 남는다).
  화면 하단에 공개 조건(10분 주기 빌드·최대 15분 지연·급하면 [프로그램 갱신])을 적어 뒀다

## 새 관리자 콘솔 /gamma2 — 실데이터 연동(읽기) 완료 (2026-07-27)
- **무엇**: 기존 통계 관리자 `/gamma`는 유지하고, 대시보드·통계·글관리를 담는 **새 콘솔 `/gamma2`**(build.py `ADMIN2_PATH`)를 신설. 현재 **읽기 실연동까지 라이브**: 조회수=`/api/stats`, 글 목록·메타=신설 매니페스트. 기획·시안은 `admin-console/`.
- **글 메타 매니페스트**: build.py가 발행+비게시(draft) 전체 메타를 `WEB-INF/posts.json`으로 생성(draft는 `_allarts`엔 담고 `arts`(사이트 생성)에선 제외 → 사이트 출력 무변경). 비게시 글의 제목·존재는 **인증 뒤에서만** 노출(WEB-INF는 웹서빙 차단, JSP 관문이 `pwEquals`).
- **쓰기(저장·삭제·온오프)는 아직**: GitHub 쓰기 PAT + 인증보강(쿼리스트링 비번 폐지) 결정·구현 후. 현재 쓰기 버튼은 "다음 단계" 안내만.
- **⚠ 잠복 인프라 사실 2건(재발 주의)**:
  1. **Apache는 `/api/`를 통째 프록시하지 않는다** — 서버 실설치본은 `/api/track`·`/api/stats`·`/api/update`만 개별 ProxyPass. 그래서 신설 `/api/posts`는 **Apache 404**. 매니페스트를 이미 프록시되는 `/api/stats?posts=1` 분기에 얹어 우회함. `posts.jsp`는 남겨둠 — **새 `/api/*` 엔드포인트를 실제로 쓰려면 서버담당자가 `deploy/apache-blog.conf`(blanket `/api/` 규칙 이미 반영됨)를 재적용+Apache reload** 해야 함.
  2. **JSP만 고친 배포는 서버에 반영 안 됨** — `/api/update`의 `git reset`은 내용이 바뀐 파일만 갱신 → web.xml mtime 불변 → Resin(`redeploy-mode automatic`) 웹앱 리로드 미발동 → **구 컴파일본 서빙**. web.xml의 `deployRev` 값을 올려 내용을 바꾸면 리로드가 걸려 JSP 재컴파일됨. **JSP 수정 배포 시 web.xml `deployRev`를 반드시 함께 올릴 것.**
- **보안**: `/api/*` 인증이 아직 기본 비번 `gamma`(쿼리스트링). 쓰기 붙이기 전 반드시 교체+세션토큰화.

## 신규 콘텐츠 배치 (2026-07-22 확정, 팀장 안기범 큐레이션)

07-15 '추가주제 10선'은 **10편 전량 발행 완료**. 아래가 다음 5선이다. 배합은 취향이 아니라 산수로 결정됐다 — 28편 시점에 목표(50/35/15)에 닿으려면 **TOFU 4 + BOFU 1**이 유일해에 가깝다.

| 순위 | 주제 | 카테고리·퍼널 | 상태 |
|---|---|---|---|
| 1 | 민간자격증 등록 방법, 협회·기관이 처음 해야 하는 일 | certification·TOFU | **발행 완료(07-22)** |
| 2 | 서버 없이 자체 교육 영상 플랫폼 구축하기 (업로드→진도율 연동) | video·BOFU | 집필 중 |
| 3 | 신입사원 온보딩 교육, LMS로 설계하는 법 | hrd·TOFU | 대기 — **시즌성 7~9월** |
| 4 | 블렌디드 러닝 설계 가이드 (집합 출결 + 온라인 진도) | edutech·TOFU | 대기 |
| 5 | 교육 영상, 직접 만들까 사올까 (자체제작·구매·임대) | video·TOFU | 대기 |

- **다음 배치 예약**: news(CSAP·신뢰자산) BOFU 1편 + 「클라우드 LMS 보안·개인정보 심사 통과 가이드」(MOFU). 후자는 근거가 가장 탄탄한데 이번 배합에 자리가 없어 유예한 것이다.
- 5선 근거·카니발라이제이션 판정·내부링크 설계는 대화 이력 기준이며, 재판단이 필요하면 팀장(b1)에게 다시 물을 것.

## 프로그램 갱신 버튼 추가 (2026-07-23)
- **배경**: 회사 환경상 cron 자동 갱신 적용이 어렵다는 COO 판단 → 서버가 최초 1회만 당겨오고 이후 멈춘 상태였음(실측: 홈은 07-16 빌드, 07-22 이후 새 글 404). DNS·서버 설치는 이미 완료돼 있었음(자체서버가 서빙 중)
- **결정**: cron 대신 `/gamma` 대시보드에 **[프로그램 갱신] 버튼** → `POST /api/update` → 서버에서 `git fetch` + `git reset --hard origin/deploy` 실행, 결과를 화면에 표시. 소스 `server/WEB-INF/jsp/update.jsp`
- **d2 적대 검토 반영**: ① pull(merge)은 이 배포모델과 충돌 → fetch+reset로 교정 ② 익명스레드의 non-final 캡처(Java 6/7 컴파일 실패 위험) → 로컬 final ③ 손자 프로세스 잔존 대비 destroyForcibly 2차 종료 ④ 기본 비번 `gamma`가 이제 배포 트리거까지 여는 문제 → 문서에 교체 경고 강화
- **안전장치**: 비번 인증·POST 전용·셸 미경유(인자 배열)·60초 타임아웃·동시 1건·`updateEnabled` 스위치. Apache는 `/api/update`만 timeout=90으로 별도 프록시
- **남은 것**: 서버에서 Resin 계정의 `/home/blog` git 쓰기 권한 확인 + 비번 교체(`admin.pw`). JSP 실측(`curl -X POST /api/update` → 401)은 배포 후 첫 확인

## 자체서버 대응 완료 (2026-07-22)
- **확정 환경**: Rocky Linux 9.5 + Apache 2.4.62 + Resin 4.0.67. git 루트 `/home/blog` · 웹루트 `/home/blog/public_html` · 데이터 `/home/blog/data`
- **배포 경로 확정**: `main` push → Actions 빌드 → `deploy` 브랜치(`public_html/` 아래) → 서버 cron 2분마다 `git fetch && git reset --hard`. **rsync 불필요**(브랜치 트리 = `/home/blog` 모양)
- **조회수/`admin` 통계 부활**: Netlify Functions+Blobs 종속을 끊고 **Resin JSP로 재구현**(`server/WEB-INF/`). `/api/track`·`/api/stats`, 저장은 웹루트 밖 `/home/blog/data/stats.tsv`. 대시보드 응답 형태는 그대로라 `/admin` 코드 무수정
- **산출물**: `deploy/apache-blog.conf`(vhost·WEB-INF 차단·/api → Resin 프록시) · `deploy/resin-blog.xml`(host/web-app) · `deploy/서버담당자-설치가이드.md`(SELinux·권한·cron 포함). nginx 설정은 폐기
- **레포 공개 전환·`deploy` 브랜치 생성 완료(07-22)**: 익명 접근 실측 확인(GitHub API 200, raw 200). 브랜치 최상위는 `public_html/` 하나로 정리됨(옛 평면 구조 잔재 제거)
- **관리자 대시보드 경로 `/admin` → `/gamma`(07-22)**: `build.py`의 `ADMIN_PATH` 상수 한 곳에서 관리. `track.jsp`의 집계 제외 조건은 서버 코드라 하드코딩 — **경로 변경 시 두 곳을 같이 고칠 것.** robots.txt의 `Disallow`는 제거(경로를 광고하는 꼴이라), 색인 차단은 페이지 meta noindex로
- **잠복 결함 3건 동시 수정(07-22)**: ① `deploy.yml`의 `paths-ignore: '**.md'`가 `articles/*.md`까지 걸러 **발행 push가 통째로 무시**되던 문제 → `'*.md'`로 축소 ② 건전성 검사에 WEB-INF 확인 추가(빠지면 `rsync --delete`가 서버 API를 지우고도 초록불) ③ 원고 수 대비 페이지 수 검사로 교체
- **남은 것**: ① 서버 담당자 설치 ② DNS를 호스트잇에서 자체서버 A레코드로 + SSL

## 배포 이전 결정 (2026-07-16, 이력)
- **결정**: Netlify·Cloudflare 둘 다 버리고 **회사 자체 웹서버로 이전.** 작업일 **2026-07-21(화).**
- **배경**: Netlify 무료 크레딧 소진(이번주 가입, 리셋 ~1개월) → 프로덕션 배포 동결. Cloudflare는 외부 DNS(호스트잇) 커스텀도메인 검증 벽(과거 2차도메인 연결 실패 원인).
- **화요일까지 방침**: 아무것도 손대지 않고 **Netlify에 그대로 얹어만 둔다.** 추가 배포·크레딧 조치·수선 없음. 최신 반영은 07-21 자체서버 이전 때 한 번에.
  - `blog.malgnsoft.com` → **Netlify(merry-snickerdoodle-74abba.netlify.app)** CNAME, **07-14 옛 빌드에 동결**된 채 유지(의도된 방치).
  - 최신본(23편·메뉴개편·에듀테크 새글+커버)은 **https://malgnblog.freerahn6.workers.dev (Cloudflare Workers)** 에 미리보기로만 존재 — 유지보수 안 함.
- **이전 시 체크리스트(화요일 준비물)**: ① 자체서버 정적 서빙 경로(빌드 산출물 `_deploy/public` 업로드) ② `blog.malgnsoft.com` DNS를 호스트잇에서 자체서버 **A레코드**로 변경 + SSL(Let's Encrypt 등) ③ 조회수/`admin` 통계는 Netlify Functions+Blobs 종속 → 자체서버용으로 재구현 필요(또는 무료 분석 대체) ④ 네이버/구글 인증 메타·robots·sitemap 도메인 그대로 유지.

## 지금 진행 중
- 무엇: 총 **40편** 발행(08-13 빌드 실측) — 10선·5선 계획 소진 후 **신규 발굴 국면**(AI LMS 07-28, 영상보안 07-29). 영상보안은 위캔디오 실검색데이터(바탕화면 `기타` 키워드 csv/xlsx) 근거로 video 클러스터 고volume 빈칸을 메운 것. 위캔디오 OTT+LMS BOFU는 여전히 draft(진도율 이슈) + 자체서버 설치 대기 + 색인 안착 대기
- 다음 게이트: ① **서버 담당자 설치 + DNS 전환** ② 신규 유입 키워드 후속(블렌디드 러닝 등 미집필分) ③ 서치콘솔/네이버 색인 확인
- ⚠ 유입 키워드 참고(대화 이력): 최고볼륨 법정의무교육류는 legal-mandatory-training-online이 선점 → 새 글보다 시즌 리프레시가 SEO 정석. 블렌디드 러닝(TOFU·미집필)이 안전한 다음 후보

## 최근 완료 (5~7개만)
- 신규 발행(08-13): 「협회·학회 교육운영에 LMS가 필요한 이유」(lms·**MOFU**, 강이슬, slug `association-society-training-lms`). 기존 `association-integrated-operation`(certification·BOFU·회원/교육/자격 통합설계)과 각도 분리 — 이쪽은 **"왜 필요한가"**(수작업 반복 제거)를 lms 클러스터에서 다룬다. 표1(업무별 수작업 vs LMS)+SVG1(연간 교육 사이클), 내부링크 3(협회 통합운영·블렌디드·도입가이드) linkcheck 통과. 기능명은 `_product-lms.md` 실제 메뉴명만 인용, **평점·이수시간 인정 기준은 단정 회피**("기관 규정·소관 부처 고시, 발행 시점 2026-08 기준"). 커버=COO 지정 `바탕화면\기타\학회.jpg` → 720×405 크롭 후 img_map.json 등록
- 신규 발행(08-12): 「동영상 기능, 아직도 직접 개발하나요: 업로드·CMS·통계·플레이어를 한 번에 얹는 법」(video·**MOFU**, 이채영, slug `video-feature-build-vs-ovp`). **자체개발 vs OVP 임대**라는 새 각도 — 기존 video 3편(유튜브 vs 자체=where / 보안 / AI자막)과 카니발 없음. 근거는 바탕화면 `위캔디오\WECANDEO_소개서_2025.pptx`(62슬라이드 텍스트 추출). 표3(구성요소·기능대응·요금제)+SVG2(문의채널 단순화·비용곡선). **레드라인 처리**: 고객 실명 8곳 전량 업종 유형으로 치환, "국내 최초 SaaS형 OVP"는 자사 소개서 표기 전제, 요금표는 "소개서 2025 기준·변동 가능" 캡션 병기, 누적 재생/고객수 수치는 `_facts.md`와 상충 소지 있어 미사용. **draft인 video-platform-build-lms-integration(진도율 이슈)는 링크하지 않고**, 진도율은 "연동 방식을 계약 전 확인" 체크항목으로만 서술
- 신규 발행(07-29): 「영상보안 길라잡이 — DRM·워터마크·보안 플레이어 총정리」(video·**BOFU**, 이채영). 위캔디오 정면 연결글. **위캔디오 홈페이지 보안 3페이지(멀티DRM·보안플레이어 StarPlayer+·미디어보안) 팩트를 `_product-wecandeo.md` 2-4b(출처 `[홈페이지]`)에 반영 후 인용.** 위협→방어→위캔디오기능 3단 표 + 보안 4계층 SVG + 등급별 체크리스트. 커버=보안.jpg→webp. 레드라인(자사 최상급·고객수·고객실명) 전량 배제. COO 개고(제목 단축·위캔디오 연결 강화) 후 발행
- 신규 발행(07-28): 「AI 튜터·AI LMS, 교육 현장에서 실제로 뭐가 바뀌나」(edutech·TOFU, 이채영). **유입 목적** 신규 키워드(AI LMS/AI 튜터) — 10선·5선 계획 소진 후 첫 신규 발굴글. COO 지정 커버(ai.jpg→webp) img_map.json 등록. 표1·SVG1·내부링크3·FAQ3, 팩트 SSOT 준수(위캔디오 AI 수치 정성표현). COO가 HTML 샘플 검토 후 컨펌해 발행
- 신규 발행(07-22): 「민간자격증 등록 방법, 협회·기관이 처음 해야 하는 일」(certification·TOFU, 한다현). 제도가 본체인 글이라 **감마가 1차 출처 대조 검증**(PQI 통계·자격기본법 제17·33조·공인 요건 전부 일치). 근거 없는 등록면허세·접수창 규정·체감 소요기간은 미기재. 커버는 지정 이미지 사용
- 자체서버 이전 대응 일체(07-22): Apache/Resin 설정·설치가이드·조회수 JSP 재구현·배포 파이프라인 구조 변경 (위 섹션 참조)
- 관리자 경로 `/admin` → `/gamma`(07-22)
- 신규 발행(07-16): 「2026 에듀테크 트렌드 5가지와 운영자 대응 전략」(edutech·TOFU, 이채영)
- 상단 메뉴 링크 수정 + 홈/전체보기 분리(`/all/` 20개 페이징)
- 네이버 대응: RSS 피드 + robots에 Yeti 명시 + 서치어드바이저 인증 / 구글 서치콘솔 소유권 확인

## 열린 이슈
- **예약 발행 — 저장소 Secret `BLOG_ADMIN_PW` 미등록**(2026-08-04 신설). 등록 전까지 예약 시각이 와도
  서버 반영은 수동(`/gamma` [프로그램 갱신])이다. 값은 서버 `/home/blog/data/admin.pw` 와 같아야 한다.
  기본값 `gamma` 를 아직 안 바꿨다면 **교체를 먼저** 하고 그 값을 secret 으로 넣을 것
- **자체서버 설치 대기** — 담당자에게 `deploy/` 3종(설치가이드·apache-blog.conf·resin-blog.xml) 전달 필요. 그 전까지 blog.malgnsoft.com은 Netlify 07-14 옛 빌드로 동결(의도된 방치)
- **Apache↔Resin 연결 방식 미확인** — 기본은 mod_proxy(127.0.0.1:8080). 서버가 mod_caucho를 쓰고 있거나 Resin 포트가 다르면 `apache-blog.conf`에서 조정 필요(주석에 대안 명시)
- **JSP 실측 미완(JDK 없음)** — 로컬에 Java가 없어 컴파일·기동 검증 불가. 설치 시 `curl -X POST /api/track` → 204 확인이 첫 실측. 404면 `<jsp-file>`의 WEB-INF 경로 매핑 문제이므로 JSP를 `/api/` 아래로 옮기는 폴백 필요
- **대시보드 비밀번호 기본값(`gamma`)이 공개 저장소에 노출** — 설치 시 서버의 `/home/blog/data/admin.pw`로 반드시 교체. **경로도 `gamma`라 비밀번호는 경로와 다른 값으로 할 것**(가이드에 명시)
- **누적 조회수 0부터 재시작** — Netlify Blobs 데이터는 이관하지 않음(의도)
- README.md "현재 상태" 문구가 실제 진척(24편)보다 뒤처짐 → 다음 정리 시 동기화
- 콘텐츠 배합 현황(24편): TOFU 11(46%)/MOFU 10(42%)/BOFU 3(12%) — 목표 50/35/15에 한 걸음 접근. 5선 잔여 4편(BOFU 1 + TOFU 3)을 마치면 28편에서 **14/10/4 = 50.0/35.7/14.3%**로 세 축 모두 오차 1%p 이내
- 발행글 24편 중 `/contact/` 링크는 빌드가 문의 URL로 치환(`build.py`) — 깨진 링크 아님. 점검 시 오인 주의
