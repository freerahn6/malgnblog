# 맑은소프트 블로그 1차 산출물 검수 리포트 (05)

- 문서 ID: BLOG-QA-01 · 작성 2026-07-10 · 검수: 웹/SEO/사실준수 게이트
- 검수 대상 실물: `articles/lms-selection-guide-for-corporate-training.md`(샘플①), `articles/refund-training-lms-operation-guide.md`(샘플②), `articles/certification-system-build-guide.md`(샘플③)
- 기준 문서: `_facts.md`(SSOT·최우선), `_taxonomy.md`, `_brief.md`, `01_마케팅_SEO전략.md`, `03_기획_IA구조.md`, `04_홍보_톤앤매너.md`, `02_콘텐츠캘린더.md`
- 검수 방식: 3편 전문 대조 + 금지어 전수 grep + 길이/구조 계량 스크립트 + 자산 파일 존재 확인(`find`)

---

## 0. 종합 판정: **조건부 발행 — 단, 샘플①은 CSAP 해소 전 발행 보류**

- **샘플①(LMS 선택)**: **보류(Hold).** 레드라인(CSAP 획득 단정) 1건 존재 → COO 확인·삭제 전 발행 불가.
- **샘플②(환급과정)**: **조건부 발행.** 발행일·H1 중복·상호링크 slug만 고치면 발행 가능. 규제 회피 처리 우수.
- **샘플③(자격검정)**: **조건부 발행.** 최소 수정(이미지 경로/썸네일)만으로 발행 가능. 사실준수·규제 회피 우수.

레드라인(무근거 인증 표방)이 1편에 있어 3편 일괄 발행은 불가. 결함 대부분은 **국소적(라인 단위)**이라 감마 반영 시 빠르게 조건부 통과 가능.

### Must-fix (우선순위)
| # | 심각도 | 축 | 파일/라인 | 요약 |
|---|---|---|---|---|
| **M1** | **치명(레드라인)** | 사실준수 | 샘플① L74 | CSAP(클라우드 보안인증) **획득 단정** — `_facts` 금지 항목 |
| **M2** | 높음 | 내부링크 | 샘플① L141 / 샘플② L169 | 상호 참조 slug 불일치 → **404 2건** |
| **M3** | 높음 | front matter/캘린더 | 샘플② L7·L8·L173 | 발행일 `07-18`(토) ≠ 캘린더 `07-17`(금) |
| **M4** | 중 | front matter 정합 | 샘플①③ / 샘플① | `thumbnail` 누락(①③)·`updated` 누락(①)·`author` 값 불일치(①) |
| **M5** | 중 | SEO 구조 | 샘플② L22 | 본문 `#` H1 → 템플릿 H1과 **중복 H1** |
| **M6** | 중 | 이미지/웹표준 | 3편 전체 | 참조 이미지 **파일 부재 + 플레이스홀더 미표기** + 경로/포맷 규칙 위반(①③) |

### Nice-to-have
- 표 래퍼를 `03 §5.1` 표준(`<figure class="table-wrap" role="region" aria-label tabindex="0">`)으로 통일, `<th scope>` 부여(샘플① 표 누락).
- 이미지에 `width/height/loading/decoding` 부여(CLS 방지, 3편 전부 없음).
- FAQ 본문 노출 방식 통일(샘플①만 본문 FAQ 有 / ②③ 無).
- 고객 규모 표기 통일: 샘플① "300여 개" vs 샘플③ "약 375개" (둘 다 `_facts` 허용이나 브랜드 일관성).
- 제목 길이: 샘플① 44자(브랜드접미사 포함 시 ~55자) — 30~40자 권장 상단, 축약 검토.

---

## 1. 항목별 판정표

### 1) 사실준수(_facts) — 최우선
| 파일 | 판정 | 근거(라인/인용) | 조치 |
|---|---|---|---|
| 샘플① | **실패** | L74: "공공 영역이라면 클라우드 보안인증(CSAP…) … **(맑은소프트는 공공기관 대상 클라우드 보안인증을 획득한 LMS를 운영합니다.)**" | M1 참조. `_facts` ⛔ "CSAP 인증 보유 근거 = COO 확인 필요 → 확정 전 본문 사용 금지" 위반. **삭제**. |
| 샘플② | 통과 | 금지 수치·표방 0건. 성능·성과 수치 미사용. | — |
| 샘플③ | 통과 | 금지어 0건. L120 "약 375개 기관·기업"·"16년 노하우" = `_facts` 허용 표기. CSAP는 계획(캘린더/04)과 달리 **작가가 생략** → 양호. | — |

- 전수 grep 결과 금지어(800개사/동시접속 5,000/200%/50%/국내 유일) 본문 사용 **0건**. 유일한 위반은 샘플① CSAP.
- 샘플① L33 "동시접속 대응"은 **일반어**(수치 없음) → 허용.
- **중요 충돌 기록**: `04 §0·§2.124`는 CSAP를 "사용 가능"으로 안내(예문까지 제시)하나, **`_facts`(SSOT·11:53 최신)는 금지**. 작가는 04를 따른 것이며 과실 아님. 그러나 SSOT·표시광고 리스크 우선 → **삭제가 원칙**. 04와 _facts의 CSAP 상충은 감마·COO가 문서 레벨에서 정리 필요.

### 2) front matter 정합
| 점검 | 판정 | 근거 | 조치 |
|---|---|---|---|
| category↔cluster↔taxonomy | 통과 | ① lms/A · ② hrd/B · ③ certification/C — `_taxonomy` 1:1 일치 | — |
| slug 영문-하이픈 | 통과 | 3편 모두 `[a-z-]` 준수, 60자 이내 | — |
| date↔캘린더 | **실패(②)** | ① 07-14=캘린더 ✅ / ② **07-18** vs 캘린더 **07-17(금)** ❌(07-18=토, 화·금 발행 위반) / ③ 07-21 ✅ | M3 |
| 키 일관성 | **경고** | 키셋 상이: ①=updated·thumbnail **없음**, ②=updated·thumbnail **있음**, ③=updated 있음·thumbnail **없음** | M4 |
| author 값 | **경고** | ① `교육운영 노트 by 맑은소프트`(블로그명) vs ②③ `맑은소프트`. `03/04` 규정상 author=Organization "맑은소프트" | M4 |

### 3) SEO 기본
| 점검 | 판정 | 근거 |
|---|---|---|
| title 길이 | 통과(경계) | ① 44자 ② 39자 ③ 27자. `03`은 30~40 권장/60 max → ①만 상단 초과, 브랜드접미사 결합 시 ~55자로 60 이내 |
| meta description | 통과 | ① 107 ② 106 ③ 94자 — 155자 이내, 유니크 |
| H1/계층 | **경고(②)** | ①③ 본문 H1 0(템플릿 생성 정상) / **② 본문 H1 1개(L22) → 템플릿 H1과 중복 위험**. H2 계층 ①7·②8·③9 양호 |
| 얇은 콘텐츠 | 통과 | 공백제외 본문 ① 5,856 ② 5,174 ③ 5,192자 — 3,000자+ 충족 |
| 키워드 분포 | 통과 | 타깃키워드가 title·리드·H2에 자연 분포(LMS 선택/고용보험 환급과정/자격검정 시스템) |
| 내부링크 | **실패(②건)** | 아래 7)에서 상술 — 상호링크 slug 404 2건 |

### 4) 표/이미지 웹표준
| 점검 | 판정 | 근거 | 조치 |
|---|---|---|---|
| 데이터표 HTML `<table>` | 통과 | 3편 모두 이미지 아님, `<caption>` 有, 가로스크롤 래퍼 有 | — |
| 표 접근성 | 경고 | `03 §5.1` 표준 래퍼(`figure role=region aria-label tabindex`) 미사용(①③ `div style`, ② `div class`). 샘플① 표는 `<th scope>` 없음 | Nice-to-have |
| 이미지 alt/figcaption | 통과 | 3편 모두 서술형 alt+figcaption 존재(양호) | — |
| 이미지 경로/포맷 규칙 | **실패** | ① `./images/…webp`(`03` 규칙 `/assets/img/{cat}/{slug}/` 위반, ②③과 불일치) / ③ `…/qualification-flow-diagram.png`(슬러그 폴더 누락+PNG, `03`은 WebP·`{slug}-{설명}` 규칙) | M6 |
| 이미지 파일 존재/플레이스홀더 | **실패** | `_assets` **비어 있음**, 블로그 내 webp/png **0개** → 참조 4개 전부 **깨진 이미지**. 플레이스홀더 명시 문구 **없음** | M6 |
| CLS 속성 | 경고 | 3편 이미지 모두 `width/height/loading/decoding` 없음 | Nice-to-have |

### 5) 구조화데이터 준비(FAQPage)
| 점검 | 판정 | 근거 |
|---|---|---|
| front matter faq 존재 | 통과 | 3편 모두 faq 3문항 → `03 §3.3` FAQPage 생성 가능 |
| 본문-frontmatter 일치 | 통과/경고 | ① 본문 FAQ가 frontmatter와 동일(정합) / ②③ 본문 FAQ 섹션 없음(JSON-LD는 frontmatter로 생성되므로 스키마상 문제 없음, 단 **표시 방식 불일치**) |
| 03 JSON-LD 설계 충돌 | 통과 | author=Organization 규정 준수 필요(M4의 ① author 값만 예외) |

### 6) 톤·게이트(04·brief)
| 점검 | 판정 | 근거 |
|---|---|---|
| 운영자 렌즈 | 통과 | 3편 모두 "담당자 페인→해법" 구조, 운영 현장 착지 |
| 정보/실무글 최상급 자제 | 통과(단 M1 예외) | 무근거 "1위/유일/최고" 표방 0건. 단 샘플① CSAP는 톤이 아닌 사실 위반 |
| 규제(환급·자격) 단정 회피 | 통과(우수) | ② L28·L173 "발행 시점 기준·HRD-Net 확인" 명시 / ③ L112~114 민간자격 등록 별개·PQI 안내·개정 고지 — 04 §4.2 준수 |
| 자기소개 리드 금지 | 통과 | 3편 모두 페인 훅으로 시작 |

### 7) 내부링크 slug 정합
| 파일 | 링크 | 판정 | 정합 대상 | 조치 |
|---|---|---|---|---|
| 샘플① L141 | `/hrd/employment-insurance-refund-course/` | **불일치(404)** | 샘플② 실제 slug `/hrd/refund-training-lms-operation-guide/` | M2 |
| 샘플② L169 | `/lms/lms-guide-for-hrd/` | **불일치(404)** | 샘플① 실제 slug `/lms/lms-selection-guide-for-corporate-training/` | M2 |
| 샘플① L139·140 | `/lms/what-is-lms/`, `/lms/self-hosted-vs-cloud-lms/` | 보류(미발행) | 캘린더 07-24·07-31 예정글 | slug 레지스트리로 발행 시 확정 |
| 샘플② L161·167·168 | `/hrd/certificate-auto-issue/`, `/hrd/legal-mandatory-training-online/` | 보류(미발행) | 캘린더 08-18·07-28 예정글 | 동일 |
| 샘플③ L114·124 | `/certification/private-qualification-registration/`, `/certification/association-member-management/` | 보류(미발행) | 캘린더 09-29·08-28 예정글 | 동일 |
| 3편 CTA | `/contact/` | 통과 | `03 §2.1` 정적 페이지 | — |

- **핵심 결함**: 이미 발행 대상인 3편이 서로를 가리키는 2개 링크가 **`03`의 예시 slug(`lms-guide-for-hrd`, `employment-insurance-training-lms`)와 실제 shipped slug가 달라** 깨짐. 카테고리 규칙(`/{category}/{slug}/`) 자체는 3편 모두 준수. 문제는 **slug 실값 불일치**.
- **권고**: front matter slug를 정본으로 하는 **slug 매핑 레지스트리(제목→category/slug)**를 감마가 관리, 모든 내부링크를 레지스트리 대조 후 삽입. 미발행글 링크는 발행 시 재검증.

---

## 2. Must-fix 상세 수정안 (무엇을 → 무엇으로)

**M1 (레드라인·최우선) — 샘플① L74**
- 현재: `데이터 암호화, 접근 권한 관리, 그리고 공공 영역이라면 클라우드 보안인증(CSAP, Cloud Security Assurance Program) 같은 인증 보유 여부를 확인하세요. (맑은소프트는 공공기관 대상 클라우드 보안인증을 획득한 LMS를 운영합니다.)`
- 수정: 괄호 안 **자사 CSAP 획득 단정 문장 전체 삭제**. 일반론만 유지 →
  `데이터 암호화, 접근 권한 관리, 그리고 공공 영역이라면 클라우드 보안인증(CSAP, 클라우드컴퓨팅 보안인증) 같은 인증 보유 여부를 공급사에 확인하세요.`
- (CSAP 자사 보유를 쓰려면 COO 확인 후 `_facts` 갱신 → 그때도 정보/TOFU글엔 `04 §0.1`에 따라 자제, 홍보글(#22)에서 근거 병기.)

**M2 — 상호링크 slug 교정**
- 샘플① L141: `/hrd/employment-insurance-refund-course/` → `/hrd/refund-training-lms-operation-guide/`
- 샘플② L169: `/lms/lms-guide-for-hrd/` → `/lms/lms-selection-guide-for-corporate-training/`

**M3 — 샘플② 발행일**
- L7 `date: "2026-07-18"` → `"2026-07-17"`, L8 `updated: "2026-07-18"` → `"2026-07-17"`, 본문 L173 `발행 시점(2026-07-18)` → `(2026-07-17)`. (캘린더 화·금 준수 = 07-17 금)

**M4 — front matter 정합**
- 샘플①③에 `thumbnail: "/assets/img/{category}/{slug}/cover.webp"` 추가(og:image/JSON-LD image 소스).
- 샘플①에 `updated: "2026-07-14"` 추가(sitemap lastmod).
- 샘플① `author: 교육운영 노트 by 맑은소프트` → `author: "맑은소프트"` (블로그명은 사이트 설정에서, JSON-LD author는 Organization). 3편 키 순서·인용부호 스타일도 `03 §3.1` 계약으로 통일 권고.

**M5 — 샘플② 중복 H1**
- L22 `# 고용보험 환급과정, LMS로 이렇게 운영한다 — 수강신청부터 수료증까지` **행 삭제**(H1은 템플릿이 frontmatter title로 생성 — `03 §4.4`). ①③처럼 본문은 리드 문단부터 시작.

**M6 — 이미지 자산/경로**
- 3편 참조 이미지(총 4개) 파일이 없음 → 발행 전 **실제 WebP 제작** 또는 **플레이스홀더 명시**(예: `<figure>`에 `data-placeholder` + 캡션에 "이미지 준비 중" 표기, 또는 이미지 블록 임시 제거).
- 샘플① `./images/lms-checklist-dashboard.webp` → `/assets/img/lms/lms-selection-guide-for-corporate-training/lms-selection-guide-for-corporate-training-dashboard.webp`.
- 샘플③ `/assets/img/certification/qualification-flow-diagram.png` → `/assets/img/certification/certification-system-build-guide/certification-system-build-guide-flow.webp` (슬러그 폴더 + WebP).
- 전 이미지에 `width height loading="lazy" decoding="async"` 부여(`03 §5.2`).

---

## 3. 미검증 항목(한계 명시)
- **실제 브라우저 렌더링 미수행**: 정적 SSG(Astro) 빌드·프리뷰 환경 부재로 크로스브라우저/반응형/실제 표 가로스크롤/이미지 로드는 **코드·규칙 대조로만** 판정. 실물 렌더 검증은 빌드 후 재점검 필요.
- **명도 대비(WCAG)·키보드 탐색 실측 불가**: CSS·컬러 토큰 미제공(테마 미구현) → 표 래퍼 `role/tabindex` 부재만 정적 지적.
- **CSAP·375개사·16년 등 사실 원본 재대조 불가**: `_facts` 시트 기준으로만 판정(원천 data 폴더 대조는 범위 외).
- **미발행 대상 내부링크(6개)**: 대상 글 미존재로 slug 정합을 캘린더 계획과만 대조, 실제 발행 slug 확정 시 재검증 필요.
- **JSON-LD 실제 출력물 미확인**: 템플릿(`03 §3.3`) 기준 "생성 가능" 판정이며, 빌드 산출 HTML의 스키마 유효성(Rich Results Test)은 미실행.
